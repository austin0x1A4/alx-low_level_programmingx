#include "main.h"

/**
 * 10-add adds two ints and returns result
 * @a: integer to be added to b
 * @b: integer t be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
int c;
c = a +b;
return (c);
}
